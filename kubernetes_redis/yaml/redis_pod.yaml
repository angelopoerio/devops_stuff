apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
spec:
  containers:
    -
      name: redis
      image: docker.io/redis
      ports:
        - containerPort: 6379

